node_modules
.next
.next/
out/
build/
dist/
*.tsbuildinfo

# data files (large, fetched on demand)
# SQLite database — excluded by default; remove these lines to commit a pre-seeded DB
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
data/*.sqlite-shm
# Cached Federal Register XML downloads
data/*.xml

# env
.env
.env.local
.env.*.local

# editors / OS
.DS_Store
*.log
.idea/
.vscode/

# Claude Code — keep shared launch config, ignore local settings
.claude/settings.local.json
